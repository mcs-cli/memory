{
  "name": "memory-loop-build",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/codex/main.ts --bundle --platform=node --target=node22 --format=cjs --outfile=plugins/memory-loop/runtime/codex.cjs",
    "test": "node --import=tsx --test tests/codex-plugin.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/proper-lockfile": "^4.1.4",
    "esbuild": "^0.25.0",
    "proper-lockfile": "^4.1.2",
    "smol-toml": "1.8.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
